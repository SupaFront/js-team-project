<img data-id={{id}} src= {{#if poster_path}}"https://image.tmdb.org/t/p/w500{{poster_path}}"{{else}}"https://picsum.photos/200/300
"{{/if}}
 alt="film" class="poster-img"/>      
 <div class="film-info">
        <h2 class="modal-film-title">{{title}}</h2>

        <div class="info-container">
          <div class="left-side-info">
            <p>Vote / Votes</p>
            <p>Popularity</p>
            <p>Original Title</p>
            <p>Genre</p>
          </div>

          <div class="right-side-info">
            <p><span class="mark">{{vote_average}}</span><span> / </span>{{vote_count}}</p>
            <p>{{popularity}}</p>
            <p class="original">{{original_title}}</p>
            <p>genres</p>
          </div>
        </div>

        <h3 class="about-title">About</h3>
        <p class="about-text">{{overview}}</p>
        <div class="btn-container">
          <button class="watched-btn" type="button">add to Watched</button>
          <button class="queue-btn" type="button">add to queue</button>
        </div>